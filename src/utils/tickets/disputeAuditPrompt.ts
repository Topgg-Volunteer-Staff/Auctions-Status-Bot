// The interaction handler routes buttons on the text before the first `_`, so
// this name must stay underscore-free.
export const DISPUTE_AUDIT_BUTTON = 'disputeAudit'

export const DISPUTE_AUDIT_QUESTION =
  'Was this ticket created because the reviewer made a mistake and needed to re-review?\n\nYour answer is recorded for the dispute audit, then the ticket is resolved and the transcript is sent.'
